name: Publish artifact

on:
  workflow_dispatch:
  release:
    types:
      - published

jobs:
  publish-artifact:
    permissions:
      packages: write
      contents: read
    uses: navikt/isa-felles-workflows/.github/workflows/publish-artifact.yml@main
    with:
      java-version: '21'
    secrets: inherit
