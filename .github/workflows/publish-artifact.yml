name: Publish artifact

on:
  release:
    types:
      - published

jobs:
  publish-artifact:
    permissions:
      packages: write
      contents: read
    uses: navikt/isa-felles-workflows/.github/workflows/build-artifact.yml
    with:
      java-version: '21'
    secrets: inherit
